@model RecipesApp.Models.RecipeAppliance

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>RecipeAppliance</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="RecipeId" class="control-label">Recipe</label>
                <select asp-for="RecipeId" class ="form-control" asp-items="ViewBag.RecipeId"></select>
            </div>
            <div class="form-group">
                <label asp-for="ApplianceId" class="control-label">Appliance</label>
                <select asp-for="ApplianceId" class ="form-control" asp-items="ViewBag.ApplianceId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
